{
    "version": 2,
    "builds": [
        {
            "src": "server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/v1/(.*)",
            "dest": "server.ts"
        },
        {
            "src": "/test",
            "dest": "server.ts"
        }
    ]
}